# Automated Test Suite Specification

This application uses isolated unit tests to assert the mathematical precision and priority branch coverage of the core deterministic financial auditing engine (`lib/auditEngine.ts`).

## Test Configurations & Environment
* **Testing Library:** Jest
* **Target Script:** `lib/auditEngine.ts`

### How to Run the Test Suite
Execute the following command within your local root terminal interface:
```bash
npm run test